node_modules/
dist/
.env
.env.local

# WhatsApp Web session (contains your login — never commit)
.wwebjs_auth/
.wwebjs_cache/

# Downloaded attachments (message media — never commit).
# Root-anchored so it doesn't match the src/media/ source folder.
/media/

# logs
*.log
npm-debug.log*

# OS
.DS_Store

# Agent-orchestrator test clone (secrets / cloned session / backups / scratch — never commit)
.env.test
.env.bak*
.wwebjs_auth_test/
tmp/
