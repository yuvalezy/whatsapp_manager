node_modules/
dist/
.env
.env.local

# WhatsApp Web session (contains your login — never commit)
.wwebjs_auth/
.wwebjs_cache/

# Downloaded attachments (message media — never commit).
# Root-anchored so it doesn't match the src/media/ source folder.
/media/

# logs
*.log
npm-debug.log*

# OS
.DS_Store
